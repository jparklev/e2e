# Conductor Desktop

Tauri + React UI for managing Conductor workspaces with fast tab switching.

## Features

- Multi-tab workspace switcher
- Create new workspaces (leave name blank for a city name)
- Uses the shared Rust core for workspace and repo management

## Development

```bash
npm install
npm run dev
```

To run the desktop shell:

```bash
npm run tauri dev
```
